# TODO

- `hw/1_hw.md`: добавить информацию про стартовую папку проекта.
- `hw/2_hw.md`: убрать или переработать пояснение про reset по `key`; пример, который был в черновике:

```tsx
<MoreAboutPanel
  key={`${from}-${to}`}
  from={from}
  to={to}
/>
```

При изменении `key` компонент будет размонтирован и смонтирован заново, а его внутреннее состояние сбросится к начальному значению.

- Добавить в 1_hw, чтобы раскладывали всё по определенной структуре
